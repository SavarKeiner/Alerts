<UserControl x:Class="Alerts.UI.AlertLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Alerts.UI"
             xmlns:control = "clr-namespace:Alerts.UI" 
             mc:Ignorable="d" 
             d:DesignHeight="331" d:DesignWidth="370" MinWidth="385" SizeChanged="UserControl_SizeChanged" Padding="5">

    <Border BorderThickness="2" BorderBrush="White">
        <Grid Margin="0" x:Name="grid" Background="#2b5797">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>


            <control:CellCoinHeader x:Name="Header" Grid.Row="0"></control:CellCoinHeader>
            <Canvas x:Name="CardGrid" Grid.Row="1"></Canvas>
        </Grid>
    </Border>


</UserControl>
